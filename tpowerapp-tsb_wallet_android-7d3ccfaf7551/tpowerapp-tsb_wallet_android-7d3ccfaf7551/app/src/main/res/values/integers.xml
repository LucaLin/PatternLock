<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 用在EditText的背景與圖示變化 -->
    <integer name="edit_level_normal">0</integer>
    <integer name="edit_level_correct">1</integer>
    <integer name="edit_level_error">2</integer>

    <!-- 身分證欄位長度限制 -->
    <integer name="userid_maxlength">10</integer>

    <!-- 登入密碼長度限制 -->
    <integer name="password_maxlength">16</integer>
    <integer name="username_sv_maxlength">16</integer>
    <integer name="password_sv_maxlength">16</integer>
    <integer name="password_wallet_maxlength">6</integer>

    <!-- 用在註冊頁的步驟圖示變化 -->
    <integer name="register_level_uncheck">0</integer>
    <integer name="register_level_current">1</integer>
    <integer name="register_level_checked">2</integer>

    <!-- 輸入金額長度限制 -->
    <integer name="money_amount_maxlength">10</integer>

    <!-- 驗證碼長度限制 -->
    <integer name="captcha_length">4</integer>

    <!-- 手機號碼長度限制 -->
    <integer name="cellphone_maxlength">10</integer>

    <!-- 驗證碼長度限制 -->
    <integer name="validation_code_maxlength">4</integer>

    <!-- 祝福語長度限制 -->
    <integer name="blessing_maxlength">50</integer>
    <integer name="red_envelope_max_amount_limit">100000</integer>
    <integer name="red_envelope_random_people_limit">200</integer>

    <!-- 紅包發送結果頁的祝福語行數 -->
    <integer name="red_envelope_sent_result_blessing_lines">4</integer>

    <!-- 加好友頁，搜尋手機號碼輸入長度限制 -->
    <integer name="add_friend_cellphone_maxlength">8</integer>
</resources>