<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- 多一層layout指定高度 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/account_list_item_height">

        <!-- checkbox -->
        <CheckBox
            android:id="@+id/chk_account"
            android:layout_width="@dimen/checkbox_button_size"
            android:layout_height="@dimen/checkbox_button_size"
            android:button="@drawable/btn_checkbox"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_centerVertical="true"
            android:layout_alignParentLeft="true"
            android:layout_toLeftOf="@id/chk_account"
            android:layout_marginLeft="@dimen/account_list_item_left_margin">

            <!-- 帳號 -->
            <TextView
                android:id="@+id/txt_account_no"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#404040"
                android:textSize="@dimen/text_size_medium"/>

            <!-- 銀行代碼與名稱 -->
            <TextView
                android:id="@+id/txt_bank_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#7d7d7d"
                android:textSize="@dimen/text_size_normal" />
        </LinearLayout>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/content_divider_height"
            android:background="#959595"
            android:layout_alignParentBottom="true"/>

    </RelativeLayout>

</RelativeLayout>