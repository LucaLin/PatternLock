<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start"
    tools:context="tw.com.taishinbank.ewallet.controller.MainActivity">

    <!-- 整個畫面 -->
    <android.support.design.widget.CoordinatorLayout xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <!-- 上方標題與選單 -->
        <include layout="@layout/appbar" />

        <!-- 主要畫面內容 -->
        <TabHost
            android:paddingTop="?actionBarSize"
            android:id="@android:id/tabhost"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >
            <RelativeLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="match_parent" >
                <!-- tab -->
                <TabWidget
                    android:id="@android:id/tabs"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/bottom_tab_bar_height"
                    android:showDividers="none"
                    android:background="@drawable/bg_home_tab"
                    android:layout_alignParentBottom="true"/>

                <!-- 頁面內容-->
                <FrameLayout
                    android:id="@android:id/tabcontent"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_alignParentTop="true"
                    android:layout_above="@android:id/tabs" />
            </RelativeLayout>
        </TabHost>
    </android.support.design.widget.CoordinatorLayout>

    <!-- 左側選單 -->
    <android.support.design.widget.NavigationView android:id="@+id/nav_view"
        android:layout_width="@dimen/nav_header_width" android:layout_height="match_parent"
        android:layout_gravity="start" android:fitsSystemWindows="true"
        app:menu="@menu/activity_main_drawer"
        android:background="#AA000000"
        app:itemTextColor="@android:color/white"
        app:itemIconTint="@android:color/white"/>

</android.support.v4.widget.DrawerLayout>